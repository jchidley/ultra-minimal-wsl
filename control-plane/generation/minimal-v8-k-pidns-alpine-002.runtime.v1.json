{
  "schema": 2,
  "generator": "candidate-powershell-v2",
  "phase": "runtime",
  "files": [
    {
      "name": "runner",
      "source": "C:\\Users\\jackc\\git\\ultra-minimal-wsl\\control-plane\\controlled-package-offline\\Invoke-MinimalV8KPidNsAlpineTrial.ps1",
      "output": "C:\\Users\\jackc\\git\\ultra-minimal-wsl\\control-plane\\controlled-package-offline\\Invoke-MinimalV8KPidNsAlpineTrial002.ps1",
      "source_sha256": "6175f45e9e0890d6f2d0f60c8537c9a825169d2daa38912f54df827933a748fd",
      "output_sha256": "05fba3bfbf3e21280db87b4786b2507e7162c665e5febd955ea0e73603de8da6",
      "substitutions": [
        {
          "new": "$TrialId = 'CP-MINIMAL-V8-K-PIDNS-ALPINE-002'",
          "old": "$TrialId = 'CP-MINIMAL-V8-K-PIDNS-ALPINE-001'"
        }
      ]
    },
    {
      "name": "controller",
      "source": "C:\\Users\\jackc\\AppData\\Local\\ultra-minimal-wsl\\approval-state\\minimal-v8-k-pidns-alpine-runtime-021\\Run-ControlledTrial.ps1",
      "output": "C:\\Users\\jackc\\AppData\\Local\\ultra-minimal-wsl\\approval-state\\minimal-v8-k-pidns-alpine-runtime-022\\Run-ControlledTrial.ps1",
      "source_sha256": "790275f629e939d0a64c17a7dd493972ba725327840eed03dce429aeae82f955",
      "output_sha256": "0329d11f04274488d1d9507f41a5e2278ad56d7af52c194b66df92cf7b7c57db",
      "substitutions": [
        {
          "new": "$trial = 'CP-MINIMAL-V8-K-PIDNS-ALPINE-002'",
          "old": "$trial = 'CP-MINIMAL-V8-K-PIDNS-ALPINE-001'"
        },
        {
          "new": "$registration = $null\n$remote = $null\n$cleanup = $null\n$outerFailure = $null",
          "old": "$remote = $null\n$outerFailure = $null"
        },
        {
          "new": "$runnerSource = Join-Path $project 'control-plane\\controlled-package-offline\\Invoke-MinimalV8KPidNsAlpineTrial002.ps1'",
          "old": "$runnerSource = Join-Path $project 'control-plane\\controlled-package-offline\\Invoke-MinimalV8KPidNsAlpineTrial.ps1'"
        },
        {
          "old": "$runnerHash = '6175f45e9e0890d6f2d0f60c8537c9a825169d2daa38912f54df827933a748fd'",
          "new": "$runnerHash = '05fba3bfbf3e21280db87b4786b2507e7162c665e5febd955ea0e73603de8da6'"
        },
        {
          "new": "    $registration = Invoke-Command -Session $session -ScriptBlock {\n        param($Trial)\n        Set-StrictMode -Version Latest\n        $ErrorActionPreference = 'Stop'\n        $wsl = 'C:\\Windows\\System32\\wsl.exe'\n        $name = 'Alpine-Minimal'\n        $rootfs = 'C:\\controlled-inputs\\ultra-minimal-wsl\\alpine-minirootfs-3.24.0-x86_64.tar.gz'\n        $install = 'C:\\controlled-inputs\\ultra-minimal-wsl\\registered-distros\\Alpine-Minimal'\n        $evidence = 'C:\\controlled-wsl-trials'\n        $listed = (& $wsl --list --quiet).Replace(\"`0\", '') -split \"`r?`n\"\n        if ($LASTEXITCODE -ne 0 -or $listed -contains $name) { throw 'Alpine infrastructure precondition failed: distro is already registered or list failed.' }\n        if (Test-Path -LiteralPath $install) { throw 'Alpine infrastructure install path already exists.' }\n        [IO.Directory]::CreateDirectory((Split-Path $install)) | Out-Null\n        $stdout = Join-Path $evidence \"$Trial-alpine-import.stdout.log\"\n        $stderr = Join-Path $evidence \"$Trial-alpine-import.stderr.log\"\n        $process = Start-Process -FilePath $wsl -ArgumentList @('--import',$name,$install,$rootfs,'--version','2') -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru\n        if (-not $process.WaitForExit(45000)) {\n            $killer = Start-Process -FilePath 'C:\\Windows\\System32\\taskkill.exe' -ArgumentList @('/PID',[string]$process.Id,'/T','/F') -Wait -PassThru -WindowStyle Hidden\n            $null = $process.WaitForExit(10000)\n            throw \"Alpine import timed out; taskkill exit $($killer.ExitCode), process stopped $($process.HasExited).\"\n        }\n        if ($process.ExitCode -ne 0) { throw \"Alpine import failed with exit $($process.ExitCode).\" }\n        $listed = (& $wsl --list --quiet).Replace(\"`0\", '') -split \"`r?`n\"\n        if ($LASTEXITCODE -ne 0 -or $listed -notcontains $name) { throw 'Alpine import did not create the required registered distro.' }\n        & $wsl --shutdown\n        if ($LASTEXITCODE -ne 0) { throw 'Post-import WSL shutdown failed.' }\n        [pscustomobject][ordered]@{registered=$true;distribution=$name;installPath=$install;completedUtc=[DateTime]::UtcNow.ToString('o')}\n    } -ArgumentList $trial\n    $remote = Invoke-Command -Session $session -ScriptBlock {",
          "old": "    $remote = Invoke-Command -Session $session -ScriptBlock {"
        },
        {
          "new": "finally {\n    if ($session) {\n        try {\n            $cleanup = Invoke-Command -Session $session -ScriptBlock {\n                param($Trial)\n                Set-StrictMode -Version Latest\n                $ErrorActionPreference = 'Stop'\n                $wsl = 'C:\\Windows\\System32\\wsl.exe'\n                $name = 'Alpine-Minimal'\n                $install = 'C:\\controlled-inputs\\ultra-minimal-wsl\\registered-distros\\Alpine-Minimal'\n                $evidence = 'C:\\controlled-wsl-trials'\n                $listed = (& $wsl --list --quiet).Replace(\"`0\", '') -split \"`r?`n\"\n                if ($LASTEXITCODE -ne 0) { throw 'Pre-cleanup distro list failed.' }\n                if ($listed -contains $name) {\n                    $stdout = Join-Path $evidence \"$Trial-alpine-unregister.stdout.log\"\n                    $stderr = Join-Path $evidence \"$Trial-alpine-unregister.stderr.log\"\n                    $process = Start-Process -FilePath $wsl -ArgumentList @('--unregister',$name) -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru\n                    if (-not $process.WaitForExit(45000)) {\n                        $killer = Start-Process -FilePath 'C:\\Windows\\System32\\taskkill.exe' -ArgumentList @('/PID',[string]$process.Id,'/T','/F') -Wait -PassThru -WindowStyle Hidden\n                        $null = $process.WaitForExit(10000)\n                        throw \"Alpine unregister timed out; taskkill exit $($killer.ExitCode), process stopped $($process.HasExited).\"\n                    }\n                    if ($process.ExitCode -ne 0) { throw \"Alpine unregister failed with exit $($process.ExitCode).\" }\n                }\n                & $wsl --shutdown\n                if ($LASTEXITCODE -ne 0) { throw 'Post-unregister WSL shutdown failed.' }\n                if (Test-Path -LiteralPath $install) { Remove-Item -LiteralPath $install -Recurse -Force }\n                $listed = (& $wsl --list --quiet).Replace(\"`0\", '') -split \"`r?`n\"\n                if ($LASTEXITCODE -ne 0 -or $listed -contains $name -or (Test-Path -LiteralPath $install)) { throw 'Alpine infrastructure cleanup verification failed.' }\n                [pscustomobject][ordered]@{unregistered=$true;installPathAbsent=$true;completedUtc=[DateTime]::UtcNow.ToString('o')}\n            } -ArgumentList $trial\n        }\n        catch { if (-not $outerFailure) { $outerFailure = $_ } }\n        [IO.Directory]::CreateDirectory($extract) | Out-Null\n        foreach ($name in @(\"$trial-alpine-import.stdout.log\",\"$trial-alpine-import.stderr.log\",\"$trial-alpine-unregister.stdout.log\",\"$trial-alpine-unregister.stderr.log\")) {\n            $guestPath = Join-Path 'C:\\controlled-wsl-trials' $name\n            if (Invoke-Command -Session $session -ScriptBlock { param($Path) Test-Path -LiteralPath $Path -PathType Leaf } -ArgumentList $guestPath) { Copy-Item -FromSession $session -LiteralPath $guestPath -Destination (Join-Path $extract $name) -Force }\n        }\n        Remove-PSSession $session -ErrorAction SilentlyContinue\n    }\n    if ($started) {",
          "old": "finally {\n    if ($session) { Remove-PSSession $session -ErrorAction SilentlyContinue }\n    if ($started) {"
        },
        {
          "new": "    foreach ($name in @(\"$trial.zip\",\"$recovery.zip\",\"$trial-stock-remove.log\",\"$trial-candidate-install.log\",\"$trial-candidate-remove.log\",\"$trial-stock-restore.log\",\"$trial-runner.stdout.log\",\"$trial-runner.stderr.log\",\"$trial-alpine-import.stdout.log\",\"$trial-alpine-import.stderr.log\",\"$trial-alpine-unregister.stdout.log\",\"$trial-alpine-unregister.stderr.log\")) {",
          "old": "    foreach ($name in @(\"$trial.zip\",\"$recovery.zip\",\"$trial-stock-remove.log\",\"$trial-candidate-install.log\",\"$trial-candidate-remove.log\",\"$trial-stock-restore.log\",\"$trial-runner.stdout.log\",\"$trial-runner.stderr.log\")) {"
        },
        {
          "new": "Save ([ordered]@{schema=1;registration=$registration;remote=$remote;cleanup=$cleanup;outerFailure=if($outerFailure){$outerFailure.Exception.Message}else{$null};extracted=$extracted;finalVmId=$final.Id.Guid;finalVmState=[string]$final.State;completedUtc=[DateTime]::UtcNow.ToString('o')})\nif ($outerFailure) { throw $outerFailure }\nif (-not $registration.registered -or -not $cleanup.unregistered -or -not $cleanup.installPathAbsent) { throw 'Alpine registration lifecycle requirement failed.' }\nif ($remote.stockProductState -ne 5 -or $remote.candidateProductState -eq 5 -or $remote.wslconfigExists -or $remote.diagnosticRelayCount -ne 0 -or -not $remote.recoveryResult -or -not $remote.recoveryResult.passed) { throw 'Independent stock recovery and diagnostic cleanup requirement failed.' }",
          "old": "Save ([ordered]@{schema=1;remote=$remote;outerFailure=if($outerFailure){$outerFailure.Exception.Message}else{$null};extracted=$extracted;finalVmId=$final.Id.Guid;finalVmState=[string]$final.State;completedUtc=[DateTime]::UtcNow.ToString('o')})\nif ($outerFailure) { throw $outerFailure }\nif ($remote.stockProductState -ne 5 -or $remote.candidateProductState -eq 5 -or $remote.wslconfigExists -or $remote.diagnosticRelayCount -ne 0 -or -not $remote.recoveryResult -or -not $remote.recoveryResult.passed) { throw 'Independent stock recovery and diagnostic cleanup requirement failed.' }"
        }
      ]
    }
  ]
}