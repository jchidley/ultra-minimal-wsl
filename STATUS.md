# Project status

This file contains verified present facts and the achieved boundary. `TASKS.md` owns incomplete work; `MINIMAL-BOOT-PLAN.md` owns the experiment contract. Exact candidate, artifact, operation, disposition, config, and trial identities live in committed `inventory/experiments.sqlite`; immutable evidence lives in candidate records and `recovery-harness/trials/`.

## Safety state

- Microsoft's packaged WSL 2.7.12 kernel and initrd remain the recovery baseline.
- No reduced control-plane package or candidate kernel is recorded as deployed. Every finalized candidate interval restored the pinned stock package, passed independent stock `B6-T` recovery, and returned the disposable fixture Off. This file does not assert live fixture state.
- The retained `clean-shell` checkpoint was not restored or changed.
- Disposable-fixture operations are standing-authorized only within the pinned, offline, hash-verified, plan-validated, recoverable envelope. Physical-host or shared-WSL effects require fresh explicit approval.

## Verified boundary

- The mkroot/Toybox lower bound passes QEMU `G4`.
- Stock WSL 2.7.12 passes the fixed Toybox probe at `B6-T` in `CP-STOCK-2.7.12-003`.
- Additive kernel discovery proved WSL entry (`B1`), VMBus/VSOCK (`B2`), and Hyper-V storage plus ext4 mount (`B3`). Stock system-distro overlay discovery then stopped at excluded GNS policy, selecting control-plane reduction rather than networking additions.
- Corrected `minimal-v4-stock-ns` passed `B6-T`. Its mount-only sibling reached `B3`, then closed the control path before `CreateInstanceResult`.
- `minimal-v5-mount-pid-ns` restored PID namespace semantics while continuing to omit IPC and UTS; it passed `B6-T`. The controlled delta proves PID namespace semantics are required by the tested contract. IPC and UTS namespace options remain `DEFERRED`.
- Under minimal v5, the PID-enabled storage-floor and overlay-floor reduced kernels both finalized at `B2`: mini-init exchanged configuration, then storage association failed before registered-distro mount. Under minimal v6, `K-PIDNS-001` advanced to `B3`: registered-distro association succeeded and `LaunchInit` began, then `_init` crashed with signal 11 in the known absent-overlay path before `CreateInstanceResult`.
- Minimal v6 plus `K-OVERLAY-PIDNS-001` also finalized at `B3`. Overlay removed the prior signal-11 signature, but `WslCorePort` closed after `LaunchInit` without `CreateInstanceResult`. The selected diagnostic captured 216 guest records and proved `LaunchDistro` still unconditionally moved the intentionally absent cross-distro share: ENOENT at `main.cpp:1676` entered the broad read-only/full-filesystem catch, overlay construction succeeded, then the repeated absent mount terminated `_init`.
- `minimal-v7-no-cross-distro-launch` removed only that unconditional cross-distro move and associated read-only fallback. Its two offline Linux-side builds were byte-identical. With unchanged `K-OVERLAY-PIDNS-001` and diagnostic instrumentation, the controlled package again finalized at `B3`: the prior cross-distro ENOENT and temporary-overlay signature disappeared, ext4 mounted read-write, then `WslCorePort` still closed after `LaunchInit` without `CreateInstanceResult`. The later `/proc/self/task/1/children` read failure occurs in `ErrorExit` cleanup and does not identify the initiating failure or select `CONFIG_PROC_CHILDREN`.
- Retained-path review localized the earliest minimal-v7 blocker before distro-init's first `CreateInstanceResult`: `ConfigInitializeCommon` iterates `LxssStartupCommon`, where the excluded Windows-interop `binfmt_misc` mount fails with ENOENT and throws from `ConfigInitializeEntry`. `InitEntry` catches that failure and exits, closing the inherited channel; only then does root mini-init enter `ErrorExit` and fail its separate children-file cleanup. `minimal-v8-no-binfmt-mount` removed only that startup entry. Its two pinned offline LFS-Builder outputs are byte-identical, and the controlled comparison with unchanged `K-OVERLAY-PIDNS-001` passed `B6-T`: `CreateInstanceResult` returned success, the fixed command printed `toybox-ok` and exited zero, and shutdown completed cleanly. The next controlled comparison replaced only the external kernel/config pair with the already-preserved no-overlay `K-PIDNS-001` sibling. It also passed `B6-T`: the exact fixed command printed `toybox-ok`, exited zero, and shut down cleanly. Exact stock restoration, independent stock `B6-T`, absent `.wslconfig`, zero diagnostic relays, completed protected broker, and fixture Off all passed. Overlay is therefore unnecessary for the retained Toybox contract and remains `DEFERRED`.
- The retained `K-PIDNS-001` config has exactly 30 enabled facilities beyond mkroot. The complete freeze audit records 1 as `PROVEN_WSL_REQUIRED`, 23 as `TRANSITIVE`, and 6 explicit platform/transport/storage facilities as `UNRESOLVED` at the current evidence limit. All 30 are reviewed, checked, documented, assigned to a layer and feature group, and linked to rationale and evidence; no durable classification gap remains.
- `CP-MINIMAL-V8-K-PIDNS-COLD-STARTS-001` passed the freeze-readiness acceptance gate with the unchanged retained candidate. Ten serial cold starts each used the exact fixed Toybox command, 45-second process timeout, WPR/ETW/events/crash instrumentation, and bounded shutdown; all ten printed `toybox-ok`, exited zero, reached `B6-T`, and preserved independently manifested evidence. Each candidate GuestLog had 217 records and only the already accepted single-start signature; all captured Windows events were Information-level, with no oops, panic, init crash, or new crash artifact. Exact stock restoration, independent stock `B6-T`, absent `.wslconfig`, zero relays, completed V2 broker, and fixture Off passed.
- `CP-MINIMAL-V8-K-PIDNS-ALPINE-003` established the Alpine 3.24.0 x86_64 compatibility boundary without changing the frozen candidate. The exact BusyBox/musl smoke command ran `/bin/busybox true`, verified `/proc/self/status`, `/sys`, and `/dev/null`, printed `alpine-ok`, exited zero, and reached `B6-A` within the unchanged 45-second timeout and diagnostic contract. Its 217-record GuestLog retained the accepted minimal-v8 signature, all 226 captured Windows events were Information-level, and no new crash artifact appeared. Exact stock restoration, independent Toybox `B6-T`, Alpine unregister and install-path cleanup, absent `.wslconfig`, zero relays, completed V2 broker, and fixture Off passed. Alpine therefore requires no compatibility addition and selects no `PROVEN_ALPINE_REQUIRED` facility. Two preceding attempts are preserved as infrastructure failures with no candidate result: one omitted registered-distro setup, and one rejected successful import/unregister because Windows PowerShell 5.1 exposed an empty `Start-Process` exit property.
- `CP-MINIMAL-V8-K-PIDNS-ARCH-001` established the Arch Linux 2026.07.01 x86_64 compatibility boundary without changing the frozen candidate or Alpine result. The official bootstrap source and reproducibly derived WSL-importable rootfs were pinned by hash and size. The exact glibc smoke command ran `/bin/true`, verified `/proc/self/status`, `/sys`, and `/dev/null`, printed `arch-ok`, exited zero, and reached `B6-ARCH` within the unchanged 45-second timeout and diagnostic contract. Its 217-record GuestLog retained the accepted minimal-v8 signature, all 59 captured Windows events were Information-level, and no new crash artifact appeared. Exact stock restoration, independent Toybox `B6-T`, Arch unregister and install-path cleanup, absent `.wslconfig`, zero relays, completed V2 broker, and fixture Off passed. Arch therefore requires no compatibility addition and selects no `PROVEN_ARCH_REQUIRED` facility.
- `CP-MINIMAL-V8-K-PIDNS-DEBIAN-001` established the Debian GNU/Linux 13.5 amd64 compatibility boundary without changing the frozen candidate or any prior compatibility result. Its directly WSL-importable rootfs came from the pinned Microsoft Store-signed Debian package and was cached by exact hash and size. The exact glibc smoke command ran `/bin/true`, verified `/proc/self/status`, `/sys`, and `/dev/null`, printed `debian-ok`, exited zero, and reached `B6-D` within the unchanged 45-second timeout and diagnostic contract. Its 217-record GuestLog retained the accepted minimal-v8 signature, all 59 captured Windows events were Information-level, and no new crash artifact appeared. Exact stock restoration, independent Toybox `B6-T`, Debian unregister and install-path cleanup, absent `.wslconfig`, zero relays, completed V2 broker, and fixture Off passed. Debian therefore requires no compatibility addition and selects no `PROVEN_DEBIAN_REQUIRED` facility. Two preceding attempts ended at UAC before worker start and are preserved as infrastructure failures with no candidate result; the unchanged controller then ran after the established launch-attendance notification.
- `ultra-minimal-wsl-v1` is frozen as `minimal-v8-no-binfmt-mount` plus `K-PIDNS-001`. The frozen core passes Toybox, and Alpine, Arch, and Debian all pass it unchanged with no compatibility additions.
- Every completed custom-kernel or controlled-package candidate has independent stock recovery evidence.

## Retained candidate state

- WSL source is pinned to tag 2.7.12, commit `68f601bba8eac1df20a0bbd403c6c87c92369ade`.
- The frozen passing `ultra-minimal-wsl-v1` baseline is mount plus PID namespace semantics with IPC and UTS omitted: `minimal-v8-no-binfmt-mount` plus `K-PIDNS-001`. Alpine 3.24.0, Arch Linux 2026.07.01, and Debian GNU/Linux 13.5 pass on this exact baseline with no additive kernel facility.
- The minimal-v6 plus `K-PIDNS-001`, plus `K-OVERLAY-PIDNS-001`, diagnostic, and minimal-v7 source-reduction combinations are finalized at `B3` and must not be rerun unchanged. Minimal v7 removed the diagnostic-selected cross-distro block and changed the failure signature without advancing the checkpoint. Minimal v8 removed only distro-init's excluded `binfmt_misc` startup mount and passed `B6-T` with unchanged `K-OVERLAY-PIDNS-001`, diagnostic instrumentation, and runtime contract. Its controlled `K-PIDNS-001` sibling also passed the unchanged contract, proving overlay unnecessary. Retain minimal v8 plus `K-PIDNS-001` as the current Toybox-capable boundary; both siblings are finalized and must not be rerun unchanged.
- `minimal-v6-excluded-initialize` removes only the inotify, IP-loopback, and cross-distro/GNS-DNS setup selected by the retained `Initialize` review. Its reproducible controlled package fixed the prior dynamic VHDX-association boundary. Operations 012 and 013 each restored stock, passed independent `B6-T` recovery, preserved complete evidence, and returned the fixture Off.
- The disposable fixture disk was expanded from 100 GiB to 160 GiB after a recorded capacity inspection; C: has 60+ GiB free, the `clean-shell` checkpoint is preserved, and the fixture returned Off. Build operation 007 then completed after guarded cleanup of operation 003. Runtime operations 008 and 009 stopped at UAC before worker start and touched neither fixture nor candidate. Unlaunched operation 010 was superseded after host-security review found its controller and output root user-writable. Operation 011 created the protected controller snapshot but its broker child never executed because the Program Files path was not preserved as one argument; no Hyper-V command or candidate interval began. Operations 012 and 013 completed their candidate intervals and recovery through the corrected protected broker. Diagnostic operation 014 ended before durable worker start with no mutation; fresh operation 015 completed the diagnostic and recovery. Operation 016 completed the minimal-v7 comparison and recovery. Both completed diagnostic runs removed all relays, restored absent `.wslconfig`, passed stock `B6-T`, and returned the fixture Off. Exact active identities and dispositions are queried from `inventory/experiments.sqlite`; the former JSON plan is frozen migration history.
- Two initial protected-broker launches of the minimal-v8 build controller ended at UAC before durable broker creation and caused no mutation. The launch-attendance boundary was resolved with an immediate Windows notification and a full consent window; the next hash-identical protected launch created the broker durably, completed the pinned offline build, and returned the fixture Off. The runtime broker then completed the candidate and recovery interval and returned the fixture Off.
- Do not add inotify, storage-hotplug, PCI, networking, IPC namespace, UTS namespace, or `CONFIG_PROC_CHILDREN` support without new runtime evidence.

## Evidence ownership

- Current work: `TASKS.md`.
- Contract, gates, and completion criteria: `MINIMAL-BOOT-PLAN.md`.
- Exact preparation inputs, candidate identities, operations, and terminal trials: `inventory/experiments.sqlite`.
- Kconfig classifications and generated relationship queries: `inventory/annotations.csv` and `inventory/kconfig-dependencies.sqlite`.
- Immutable runtime evidence and analysis: `recovery-harness/trials/<TRIAL_ID>/`.
- Layer-specific source and reproducibility evidence: `control-plane/candidates/`, `control-plane/*-audit.md`, and `candidates/`.
