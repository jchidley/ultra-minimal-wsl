# K-PIDNS-001 config review

Parent: `K-STORAGE-001`, full-config SHA-256 `b8d9068b9dc901ad66a4e5ff03fdfd78b02a09261ffebfdc006699b91a437bd8`.

## Evidence-selected change

| Symbol | Parent | Candidate | Relationship | Classification |
|---|---:|---:|---|---|
| `CONFIG_PID_NS` | n | y | Explicit bool; depends only on `CONFIG_NAMESPACES=y` | `PROVEN_WSL_REQUIRED` |
| `CONFIG_NAMESPACES` | y | y | Existing direct prerequisite; no selection or implication | `BASE` |

`CP-MINIMAL-V4-MOUNT-NS-001` reached B3 and closed `WslCorePort` before `CreateInstanceResult`. Under the unchanged fixed probe, `CP-MINIMAL-V5-MOUNT-PID-NS-001` restored B6-T while IPC and UTS namespace semantics remained omitted. This controlled delta selects PID namespace semantics.

Both relationship directions were queried in `inventory/kconfig-dependencies.sqlite`. `CONFIG_PID_NS` has no incoming select or imply edges and selects nothing. Its only outgoing dependency, prompt dependency, and default condition reference the already-enabled `CONFIG_NAMESPACES`. `olddefconfig` selected no additional symbols.

`CONFIG_IPC_NS=n` and `CONFIG_UTS_NS=n` remain unchanged and `DEFERRED`. `K-OVERLAY-001` is not the parent because overlay support was selected only by excluded stock-init system-distro policy; the retained reduced control plane needs the B3 `K-STORAGE-001` floor plus the newly proven PID namespace facility.

## Reproducibility and integrity

Two separate offline local clones on `LFS-Builder` ext4 generated byte-identical full configs and x86 boot kernels. The accepted config delta is exactly:

```text
 PID_NS n -> y
```

- Full config SHA-256: `e7e10f27f0b802142a2ecbb52763ba49523da61f42efc23d61bab0046d9ad563`
- Kernel SHA-256: `24c768730ba2b66e0afde5a53cbbc95e92440bb90510552ad9b9bf4ddf73c959`
- Kernel size: 3,720,192 bytes

The accepted build used the pinned kernel commit, cross-compiler, builder profile, timestamps, offline local source, and historical fail-closed `PAHOLE` override recorded in `build-metadata.txt`.
